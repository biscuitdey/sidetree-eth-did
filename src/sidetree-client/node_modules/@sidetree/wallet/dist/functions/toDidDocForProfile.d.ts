import { DidDocument, SidetreeReplaceOptions } from '../types';
export declare const toDidDocForProfile: (mnemonic: string, index: number, didMethodName: string, profile?: string, options?: SidetreeReplaceOptions) => Promise<DidDocument>;
